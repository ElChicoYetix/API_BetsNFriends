<!-- views/usuario.ejs -->

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BETS 'N' FRIENDS</title>
  <style>
    /* Estilos CSS */
    .header {
      background-color: red;
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
    }

    .header a {
      text-decoration: none;
      color: white;
      padding: 0 10px;
    }

    .container {
      max-width: 800px;
      margin: 20px auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      display: flex;
      align-items: center;
    }

    .profile {
      flex: 0 0 auto; /* No se expandirá */
      margin-right: 20px;
      text-align: center;
    }

    .profile svg {
      width: 150px;
      height: 150px;
      margin-bottom: 10px;
      color: red; /* Color del SVG igual al color del header */
    }

    .profile button {
      background-color: red; /* Color del botón */
      color: white;
      border: none;
      padding: 8px 16px;
      border-radius: 5px;
      cursor: pointer;
      display: block;
      margin-top: 10px;
      transition: background-color 0.3s; /* Transición de color al pasar el mouse */
    }

    .profile button:hover {
      background-color: #7a0000; /* Color más oscuro al pasar el mouse */
    }

    .info {
      flex: 1; /* Se expandirá para ocupar el espacio restante */
      text-align: left;
    }

    .user-info {
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <!-- Barra de navegación -->
  <div class="header">
    <div>¡Bienvenido!</div>
    <h1>BETS 'N' FRIENDS</h1>
    <div>
      <a href="/" class="login">Inicio</a> <!-- Enlace a la página de inicio -->
    </div>
  </div>

  <!-- Contenido de la página -->
  <div class="container">
    <!-- Foto de perfil y botón para cambiar -->
    <div class="profile">
      <!-- SVG de la imagen de perfil -->
      <svg xmlns="http://www.w3.org/2000/svg" width="150" height="150" fill="currentColor" class="bi bi-person" viewBox="0 0 16 16">
        <path d="M8 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6m2-3a2 2 0 1 1-4 0 2 2 0 0 1 4 0m4 8c0 1-1 1-1 1H3s-1 0-1-1 1-4 6-4 6 3 6 4m-1-.004c-.001-.246-.154-.986-.832-1.664C11.516 10.68 10.289 10 8 10s-3.516.68-4.168 1.332c-.678.678-.83 1.418-.832 1.664z"/>
      </svg>
      <button>Cambiar Foto</button>
    </div>

    <!-- Información del usuario -->
    <div class="info">
      <!-- Campo para mostrar el nombre de usuario -->
      <div class="user-info">
        <strong>Nombre de Usuario:</strong> NombreUsuario <!-- Aquí mostrar el nombre de usuario -->
      </div>
      <!-- Campo para mostrar el correo electrónico -->
      <div class="user-info">
        <strong>Correo Electrónico:</strong> correo@example.com <!-- Aquí mostrar el correo electrónico -->
      </div>
    </div>
  </div>

</body>
</html>
